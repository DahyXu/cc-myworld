# 移动端适配 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不破坏桌面版任何功能的前提下，为游戏添加手机横屏支持——左侧浮动摇杆移动、右侧滑动看视角、右下角三按钮（攻击/放块/跳跃）、5格可见快捷栏。

**Architecture:** 新增 `js/touch.js` 封装所有触控逻辑；`main.js` 顶部检测 `isMobile`，移动端跳过 Pointer Lock 路径、将 `Touch` 输出注入现有 `input` 对象和 `player.yaw/pitch`；`ui.js` 增加移动端快捷栏窗口逻辑；`index.html` 加 DOM 和 CSS，通过 `body.mobile` class 隔离样式。服务器零改动。

**Tech Stack:** 原生 Touch Events API、CSS `@media (orientation: portrait)`、Three.js WebGL（无变化）、现有 IIFE + globalThis.MyWorld 模块约定。

---

## 文件总览

| 文件 | 操作 | 职责 |
|------|------|------|
| `index.html` | 修改 | viewport meta、竖屏警告、`#mobileControls` DOM、快捷栏包裹、移动端 CSS |
| `js/ui.js` | 修改 | `buildHotbar` 加 `data-slot`、`setMobileMode`、`selectSlot` 移动端窗口 |
| `js/touch.js` | 新建 | 浮动摇杆、视角滑动、按钮事件、快捷栏滑动，暴露 `MyWorld.Touch` |
| `js/main.js` | 修改 | `isMobile` 检测、提取 `doAttack/doPlace`、`isActive()`、游戏循环接 Touch、overlay 流程、`btnE` 显隐 |

---

### Task 1: index.html — viewport / 竖屏警告 / #mobileControls DOM / 快捷栏包裹 / 移动端 CSS

**Files:**
- Modify: `index.html`

- [ ] **Step 1: 加 viewport meta 和修改现有 #hotbar CSS**

`<meta charset="utf-8">` 那行之后插入：
```html
<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">
```

把 `<style>` 内的 `#hotbar { ... }` 规则替换为：
```css
  #hotbarWrap {
    position: fixed; left: 50%; bottom: 12px; transform: translateX(-50%);
    display: flex; align-items: center; gap: 4px; z-index: 10; pointer-events: none;
  }
  #hotbar { display: flex; gap: 4px; }
  #hotbarPrev, #hotbarNext { display: none; }
```

- [ ] **Step 2: 在 `</style>` 前插入移动端 CSS**

```css
  /* ── 移动端 ── */
  body.mobile #hotbarPrev,
  body.mobile #hotbarNext {
    display: flex; align-items: center; justify-content: center;
    width: 28px; height: 44px; background: rgba(0,0,0,0.5);
    border: 1px solid #888; border-radius: 4px; color: #fff; font-size: 18px;
    pointer-events: auto; touch-action: manipulation; cursor: pointer;
  }
  body.mobile #hotbar { pointer-events: auto; }
  body.mobile .slot   { touch-action: manipulation; pointer-events: auto; }
  #orientWarn {
    display: none; position: fixed; inset: 0; z-index: 40;
    background: #111; color: #eee; font: 22px sans-serif;
    align-items: center; justify-content: center; text-align: center;
    padding: 20px;
  }
  @media (orientation: portrait) { body.mobile #orientWarn { display: flex; } }
  #mobileControls {
    display: none; position: fixed; inset: 0; z-index: 11; pointer-events: none;
  }
  body.mobile #mobileControls { display: block; }
  #joystickBase {
    position: fixed; width: 120px; height: 120px; border-radius: 50%;
    background: rgba(255,255,255,0.08); border: 2px solid rgba(255,255,255,0.3);
    pointer-events: none;
  }
  #joystickKnob {
    position: absolute; top: 50%; left: 50%; transform: translate(-50%,-50%);
    width: 48px; height: 48px; border-radius: 50%; background: rgba(255,255,255,0.5);
  }
  #btnAttack, #btnPlace, #btnJump, #btnE {
    position: fixed; display: flex; align-items: center; justify-content: center;
    font: bold 13px sans-serif; color: #fff; border: none;
    user-select: none; -webkit-user-select: none; touch-action: none; pointer-events: auto;
  }
  #btnAttack { width:56px; height:56px; background:rgba(200,50,50,0.85);  border-radius:50%;  right:70px;  bottom:28px; font-size:12px; }
  #btnPlace  { width:46px; height:46px; background:rgba(60,100,200,0.80); border-radius:10px; right:16px;  bottom:28px; font-size:11px; }
  #btnJump   { width:46px; height:46px; background:rgba(220,180,40,0.85); border-radius:50%;  right:16px;  bottom:84px; font-size:13px; color:#000; }
  #btnE      { width:50px; height:50px; background:rgba(60,180,60,0.85);  border-radius:10px; right:130px; bottom:50px; font-size:11px; display:none; }
```

- [ ] **Step 3: 修改 body 内的 DOM**

把 `<div id="hotbar"></div>` 改为：
```html
<div id="hotbarWrap">
  <button id="hotbarPrev">‹</button>
  <div id="hotbar"></div>
  <button id="hotbarNext">›</button>
</div>
```

在 `<div id="deathOverlay">` 之前插入：
```html
<div id="orientWarn">请将设备旋转为横屏 🔄</div>
<div id="mobileControls">
  <div id="joystickBase" style="display:none"><div id="joystickKnob"></div></div>
  <button id="btnJump">跳</button>
  <button id="btnAttack">攻击</button>
  <button id="btnPlace">放块</button>
  <button id="btnE">对话</button>
</div>
```

在 `<script src="js/hud.js"></script>` 之后、`<script src="js/main.js"></script>` 之前插入：
```html
<script src="js/touch.js"></script>
```

- [ ] **Step 4: 桌面验收（确保未破坏桌面版）**

用 `npx wrangler dev --port 8787 --persist-to ../cc-myworld-state` 启动本地服务器，桌面浏览器打开 http://localhost:8787，确认：
- 快捷栏仍显示在底部居中
- 左右箭头不可见（desktop 不加 .mobile class）
- 无 JS 报错

- [ ] **Step 5: 提交**

```bash
git add index.html
git commit -m "feat: 移动端HTML/CSS骨架（viewport/竖屏提示/mobileControls/快捷栏包裹）"
```

---

### Task 2: js/ui.js — slot data 属性 + setMobileMode + selectSlot 窗口逻辑

**Files:**
- Modify: `js/ui.js`

- [ ] **Step 1: buildHotbar 加 data-slot 属性**

`js/ui.js` 的 `buildHotbar` 函数里，`slot.className = 'slot';` 之后插入：
```js
      slot.dataset.slot = i;
```

- [ ] **Step 2: 加 mobileMode 状态与 setMobileMode**

`let slots = [];` 之后插入：
```js
  let mobileMode = false;
  function setMobileMode(on) { mobileMode = on; }
```

- [ ] **Step 3: 修改 selectSlot 加移动端窗口逻辑**

把原来的 `selectSlot` 函数：
```js
  function selectSlot(i) {
    slots.forEach((s, j) => s.classList.toggle('selected', j === i));
  }
```

改为：
```js
  function selectSlot(i) {
    slots.forEach((s, j) => s.classList.toggle('selected', j === i));
    if (!mobileMode) return;
    // 移动端：显示以 i 为中心的 5 格窗口（windowStart ∈ [0,5]）
    const start = Math.min(Math.max(0, i - 2), 5);
    slots.forEach((s, j) => { s.style.display = (j >= start && j < start + 5) ? '' : 'none'; });
  }
```

- [ ] **Step 4: 导出 setMobileMode**

把：
```js
  root.MyWorld.UI = { buildHotbar, selectSlot, showOverlay, setOverlayMode, getOverlayMode, setOnline };
```

改为：
```js
  root.MyWorld.UI = { buildHotbar, selectSlot, showOverlay, setOverlayMode, getOverlayMode, setOnline, setMobileMode };
```

- [ ] **Step 5: 运行全量测试（确认共享模块未破坏）**

```bash
node tests/run_all.js
```

Expected: `ALL TESTS PASSED`

- [ ] **Step 6: 提交**

```bash
git add js/ui.js
git commit -m "feat: ui.js 移动端快捷栏窗口（slot index / setMobileMode / selectSlot 窗口逻辑）"
```

---

### Task 3: js/touch.js — 新建触控模块

**Files:**
- Create: `js/touch.js`

- [ ] **Step 1: 创建 touch.js**

创建 `js/touch.js`：

```js
// js/touch.js — 移动端触控输入（浮动摇杆 / 视角滑动 / 按钮 / 快捷栏）
(function (root) {
  'use strict';

  const JOYSTICK_RADIUS = 60; // px，摇杆最大偏移半径
  const LOOK_SENS = 0.003;    // rad/px，与桌面 mousemove 系数一致

  // 摇杆状态（浮动：落指位置为圆心）
  const joy = { active: false, id: -1, cx: 0, cy: 0, dx: 0, dy: 0 };
  // 视角滑动状态
  const look = { active: false, id: -1, lx: 0, ly: 0 };
  // 视角增量缓存，由 consumeViewDelta() 每帧取走
  let dyaw = 0, dpitch = 0;
  // 跳跃键状态
  let jumpActive = false;

  // 回调（由 main.js 调用 register* 注册）
  let cbAttack = null, cbPlace = null, cbE = null, cbHotbar = null;

  function isLeftHalf(x) { return x < root.innerWidth / 2; }

  // ── 摇杆视觉更新 ──
  function syncJoystickDOM() {
    const base = root.document.getElementById('joystickBase');
    const knob = root.document.getElementById('joystickKnob');
    if (!joy.active) { base.style.display = 'none'; return; }
    base.style.display = 'block';
    base.style.left = (joy.cx - JOYSTICK_RADIUS) + 'px';
    base.style.top  = (joy.cy - JOYSTICK_RADIUS) + 'px';
    knob.style.transform = 'translate(calc(-50% + ' + joy.dx + 'px), calc(-50% + ' + joy.dy + 'px))';
  }

  // ── Canvas 触控（摇杆 + 视角） ──
  function onTouchStart(e) {
    e.preventDefault();
    for (const t of e.changedTouches) {
      if (isLeftHalf(t.clientX) && !joy.active) {
        joy.active = true; joy.id = t.identifier;
        joy.cx = t.clientX; joy.cy = t.clientY; joy.dx = 0; joy.dy = 0;
        syncJoystickDOM();
      } else if (!isLeftHalf(t.clientX) && !look.active) {
        look.active = true; look.id = t.identifier;
        look.lx = t.clientX; look.ly = t.clientY;
      }
    }
  }

  function onTouchMove(e) {
    e.preventDefault();
    for (const t of e.changedTouches) {
      if (t.identifier === joy.id) {
        let dx = t.clientX - joy.cx, dy = t.clientY - joy.cy;
        const d = Math.hypot(dx, dy);
        if (d > JOYSTICK_RADIUS) { dx = dx / d * JOYSTICK_RADIUS; dy = dy / d * JOYSTICK_RADIUS; }
        joy.dx = dx; joy.dy = dy;
        syncJoystickDOM();
      } else if (t.identifier === look.id) {
        dyaw   += (t.clientX - look.lx) * LOOK_SENS;
        dpitch += (t.clientY - look.ly) * LOOK_SENS;
        look.lx = t.clientX; look.ly = t.clientY;
      }
    }
  }

  function onTouchEnd(e) {
    for (const t of e.changedTouches) {
      if (t.identifier === joy.id) { joy.active = false; joy.dx = 0; joy.dy = 0; syncJoystickDOM(); }
      else if (t.identifier === look.id) { look.active = false; }
    }
  }

  // ── 公开 API ──

  // 前进/横移归一化向量（均在 [-1,1]）；摇杆向上 → forward 正
  function getMove() {
    if (!joy.active) return { forward: 0, strafe: 0 };
    return { forward: -joy.dy / JOYSTICK_RADIUS, strafe: joy.dx / JOYSTICK_RADIUS };
  }

  // 取走本帧视角增量（消费后清零）
  function consumeViewDelta() {
    const r = { dyaw, dpitch }; dyaw = 0; dpitch = 0; return r;
  }

  function getJump() { return jumpActive; }

  // NPC 对话按钮显隐
  function setNpcVisible(visible) {
    root.document.getElementById('btnE').style.display = visible ? 'flex' : 'none';
  }

  // 回调注册
  function registerAttack(fn) { cbAttack = fn; }
  function registerPlace(fn)  { cbPlace  = fn; }
  function registerE(fn)      { cbE      = fn; }
  function registerHotbar(fn) { cbHotbar = fn; }

  // ── 初始化（isMobile 确认后调用）──
  function init() {
    const canvas = root.document.querySelector('canvas');
    canvas.addEventListener('touchstart',  onTouchStart, { passive: false });
    canvas.addEventListener('touchmove',   onTouchMove,  { passive: false });
    canvas.addEventListener('touchend',    onTouchEnd,   { passive: false });
    canvas.addEventListener('touchcancel', onTouchEnd,   { passive: false });

    // 操作按钮
    root.document.getElementById('btnAttack').addEventListener('touchstart', (e) => {
      e.preventDefault(); if (cbAttack) cbAttack();
    }, { passive: false });

    root.document.getElementById('btnPlace').addEventListener('touchstart', (e) => {
      e.preventDefault(); if (cbPlace) cbPlace();
    }, { passive: false });

    root.document.getElementById('btnJump').addEventListener('touchstart', (e) => {
      e.preventDefault(); jumpActive = true;
    }, { passive: false });
    root.document.getElementById('btnJump').addEventListener('touchend', () => { jumpActive = false; });
    root.document.getElementById('btnJump').addEventListener('touchcancel', () => { jumpActive = false; });

    root.document.getElementById('btnE').addEventListener('touchstart', (e) => {
      e.preventDefault(); if (cbE) cbE();
    }, { passive: false });

    // 快捷栏箭头
    root.document.getElementById('hotbarPrev').addEventListener('touchstart', (e) => {
      e.preventDefault(); if (cbHotbar) cbHotbar(-1);
    }, { passive: false });
    root.document.getElementById('hotbarNext').addEventListener('touchstart', (e) => {
      e.preventDefault(); if (cbHotbar) cbHotbar(1);
    }, { passive: false });

    // 快捷栏：统一在 hotbarWrap 处理（tap=选格，swipe>40px=切格，避免 touchstart 误触发选格）
    let hbId = -1, hbStartX = 0, hbLastX = 0, hbStartTarget = null;
    const hw = root.document.getElementById('hotbarWrap');
    hw.addEventListener('touchstart', (e) => {
      e.preventDefault(); e.stopPropagation();
      const t = e.changedTouches[0];
      hbId = t.identifier; hbStartX = hbLastX = t.clientX;
      hbStartTarget = t.target.closest('[data-slot]');
    }, { passive: false });
    hw.addEventListener('touchmove', (e) => {
      e.preventDefault(); e.stopPropagation();
      for (const t of e.changedTouches) {
        if (t.identifier !== hbId) continue;
        const dx = t.clientX - hbLastX;
        if (Math.abs(t.clientX - hbStartX) > 40) {
          if (cbHotbar) cbHotbar(dx < 0 ? 1 : -1);
          hbStartX = hbLastX = t.clientX; // 重置防连发
        } else { hbLastX = t.clientX; }
      }
    }, { passive: false });
    hw.addEventListener('touchend', (e) => {
      e.preventDefault(); e.stopPropagation();
      for (const t of e.changedTouches) {
        if (t.identifier !== hbId) continue;
        // 总位移 < 10px 视为点击某格
        if (Math.abs(t.clientX - hbStartX) < 10 && hbStartTarget) {
          if (cbHotbar) cbHotbar('select', parseInt(hbStartTarget.dataset.slot));
        }
        hbId = -1;
      }
    }, { passive: false });
  }

  root.MyWorld = root.MyWorld || {};
  root.MyWorld.Touch = { init, getMove, consumeViewDelta, getJump, setNpcVisible,
    registerAttack, registerPlace, registerE, registerHotbar };
})(typeof self !== 'undefined' ? self : globalThis);
```

- [ ] **Step 2: 提交**

```bash
git add js/touch.js
git commit -m "feat: touch.js 触控模块（浮动摇杆/视角滑动/按钮/快捷栏滑动）"
```

---

### Task 4: js/main.js — isMobile 接入、doAttack/doPlace 提取、游戏循环改造、overlay 流程

**Files:**
- Modify: `js/main.js`

- [ ] **Step 1: 顶部加 isMobile 检测 + Touch 引用**

在 IIFE 顶部 `const MW = root.MyWorld;` 行之前插入：
```js
  const isMobile = 'ontouchstart' in root || root.navigator.maxTouchPoints > 1;
  if (isMobile) root.document.body.classList.add('mobile');
```

在模块引用区（`const Combat = MW.Combat, Hud = MW.Hud, QuestsDef = MW.QuestsDef;`）之后插入：
```js
  const Touch = isMobile ? MW.Touch : null;
```

- [ ] **Step 2: 加 isActive() 函数**

`function isLocked() { ... }` 之后插入：
```js
  // 移动端无 Pointer Lock；world 存在即视为激活
  function isActive() { return isLocked() || (isMobile && !!world); }
```

- [ ] **Step 3: 提取 doAttack() 和 doPlace() 函数**

在 `document.addEventListener('mousedown', ...)` 之前插入：
```js
  function doAttack() {
    if (!world || selfDead) return;
    const d0 = viewDir();
    const eye = { x: player.x, y: player.y + Player.EYE, z: player.z };
    const consumed = Combat.onAttackClick(hotbarIndex, eye, d0, Entities.mobList(), Net);
    if (consumed === 'shoot') { Entities.spawnLocalArrow(eye.x, eye.y, eye.z, d0.x, d0.y, d0.z); return; }
    if (consumed) return;
    const r = Raycast.cast(world, player.x, player.y + Player.EYE, player.z, d0.x, d0.y, d0.z, REACH);
    if (!r.hit) return;
    world.setBlock(r.x, r.y, r.z, 0);
    Net.send({ t: 'edit', x: r.x, y: r.y, z: r.z, id: 0 });
  }

  function doPlace() {
    if (!world || selfDead || Combat.ITEMS[hotbarIndex].kind !== 'block') return;
    const d = viewDir();
    const r = Raycast.cast(world, player.x, player.y + Player.EYE, player.z, d.x, d.y, d.z, REACH);
    if (!r.hit) return;
    const tx = r.x + r.nx, ty = r.y + r.ny, tz = r.z + r.nz;
    if (ty < 0 || ty >= World.CHUNK_Y) return;
    const overlap = !(tx + 1 <= player.x - Player.HALF || tx >= player.x + Player.HALF ||
                      ty + 1 <= player.y || ty >= player.y + Player.HEIGHT ||
                      tz + 1 <= player.z - Player.HALF || tz >= player.z + Player.HALF);
    if (overlap) return;
    const id = Combat.ITEMS[hotbarIndex].id;
    world.setBlock(tx, ty, tz, id);
    Net.send({ t: 'edit', x: tx, y: ty, z: tz, id });
  }
```

把原 `document.addEventListener('mousedown', ...)` 整段替换为：
```js
  document.addEventListener('mousedown', (e) => {
    if (isMobile || !isLocked() || !world || selfDead) return;
    if (e.button === 0) doAttack();
    else if (e.button === 2) doPlace();
  });
```

- [ ] **Step 4: 修改主循环 + Touch 接入**

游戏循环里，把 `if (world && player) {` 块内的第一行：
```js
      if (isLocked() && !selfDead) Player.update(player, world, dt, input);
```
改为：
```js
      if (isMobile && Touch) {
        const mv = Touch.getMove();
        input.forward = mv.forward >  0.15;
        input.back    = mv.forward < -0.15;
        input.left    = mv.strafe  < -0.15;
        input.right   = mv.strafe  >  0.15;
        input.jump    = Touch.getJump();
        if (player) {
          const vd = Touch.consumeViewDelta();
          player.yaw   -= vd.dyaw;
          player.pitch -= vd.dpitch;
          const lim = Math.PI / 2 - 0.01;
          if (player.pitch >  lim) player.pitch =  lim;
          if (player.pitch < -lim) player.pitch = -lim;
        }
        Touch.setNpcVisible(nearNpc());
      }
      if (isActive() && !selfDead) Player.update(player, world, dt, input);
```

- [ ] **Step 5: 修改 startWorld 和 softReset 的 overlay 流程**

`startWorld` 末尾的 `UI.setOverlayMode('start');` 改为：
```js
    if (isMobile) UI.showOverlay(false); else UI.setOverlayMode('start');
```

`softReset` 内的 `UI.setOverlayMode('start');` 改为：
```js
    if (isMobile) UI.showOverlay(false); else UI.setOverlayMode('start');
```

- [ ] **Step 6: 修改 openNpcDialog / closeNpcDialog 移动端路径**

`openNpcDialog()` 末尾：
```js
    pendingNpc = true;
    if (root.document.pointerLockElement) root.document.exitPointerLock();
    else { pendingNpc = false; UI.setOverlayMode('npc'); }
```
改为：
```js
    if (isMobile) { UI.setOverlayMode('npc'); return; }
    pendingNpc = true;
    if (root.document.pointerLockElement) root.document.exitPointerLock();
    else { pendingNpc = false; UI.setOverlayMode('npc'); }
```

`closeNpcDialog()` 内：
```js
    UI.setOverlayMode('start');
```
改为：
```js
    if (isMobile) UI.showOverlay(false); else UI.setOverlayMode('start');
```

- [ ] **Step 7: 初始化 Touch + 快捷栏切换接线**

在 `UI.buildHotbar(atlas, Combat.ITEMS);` 之后插入：
```js
  if (isMobile && Touch) {
    UI.setMobileMode(true);
    UI.selectSlot(0); // 触发窗口初始渲染（只显示前 5 格）
    Touch.init();
    Touch.registerAttack(doAttack);
    Touch.registerPlace(doPlace);
    Touch.registerE(() => { if (world && !selfDead && nearNpc()) openNpcDialog(); });
    Touch.registerHotbar((dirOrSelect, idx) => {
      if (dirOrSelect === 'select') {
        hotbarIndex = idx;
      } else {
        hotbarIndex = (hotbarIndex + dirOrSelect + 10) % 10;
      }
      UI.selectSlot(hotbarIndex);
      Combat.setHeld(hotbarIndex);
    });
  }
```

- [ ] **Step 8: 确认全量测试通过**

```bash
node tests/run_all.js
```

Expected: `ALL TESTS PASSED`

- [ ] **Step 9: 提交**

```bash
git add js/main.js
git commit -m "feat: main.js 移动端接入（isMobile/Touch/isActive/doAttack/doPlace/overlay流程）"
```

---

### Task 5: 浏览器验收（DevTools + 真机）

**Files:** 无代码改动

- [ ] **Step 1: DevTools 横屏模拟验收**

启动 dev：`npx wrangler dev --port 8787 --persist-to ../cc-myworld-state`

Chrome → F12 → Device Toolbar → 选 iPhone 14 Pro Max → 横屏模式，打开 http://localhost:8787。

逐项确认：
1. 左下角出现虚拟摇杆外环（触摸后），右侧可滑动转视角
2. 右下角「攻击」「放块」「跳」三按钮可见
3. 快捷栏只显示 5 格 + 左右 ‹ › 箭头
4. 输入昵称点进入，overlay 消失，进入游戏画面
5. 拖动摇杆角色可移动，视角滑动正常
6. 竖屏时出现「请旋转设备」提示，横屏后消失

- [ ] **Step 2: 桌面版回归验收**

同一台电脑桌面浏览器打开 http://localhost:8787：
- 快捷栏 10 格全部可见，箭头隐藏
- 鼠标锁定正常，WASD 移动，左键攻击，右键放块
- 无 JS 报错

- [ ] **Step 3: 部署并提交验收结论**

```bash
npx wrangler deploy
```

部署后手机浏览器打开线上地址，确认移动端完整流程可玩（移动 / 攻击 / 放块 / 跳跃 / 快捷栏 / 接任务）。

- [ ] **Step 4: 提交**

```bash
git add README.md   # 若需更新 README 说明移动端支持
git commit -m "docs: README 补移动端支持说明（横屏/虚拟摇杆）"
```

---

## 已知取舍（有意设计）

- 浮动摇杆：每次落指重置圆心，多次快速换手位置时摇杆会跳动，属正常行为
- 攻击/放块需准星对准（十字准星保留）：与桌面版体验一致，不做自动瞄准
- iOS Safari `screen.orientation.lock` 受限：仅靠 CSS media query 提示旋转，不强制锁横屏
- 快捷栏格子点击依赖 `data-slot` 属性，由 `buildHotbar()` 写入（Task 2 已处理）
- 移动端不触发 Pointer Lock：`isActive()` = `isMobile && !!world` 替代 `isLocked()` 作为输入开关
